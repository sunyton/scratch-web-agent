class wifi {

    constructor () {

    }

    exeCmd (type) {
        switch (type) {
            case "getWifiList":
                
                break;
            case "exec":

                break
            default:
                break;
        }
    }
}